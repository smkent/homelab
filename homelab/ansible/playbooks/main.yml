- name: Provision homelab host
  hosts: all
  roles:
  - system
  - auth
  - deploy_user
  - crypt_root
  - docker
  - mta
  - packages
  - users
  - apps
  - composeapps
  - name: helmet
    when: helmet_role|d(false)
  become: true
  become_user: deploy
  become_method: sudo
